# Copy to .env (gitignored) and fill in.

# ElevenLabs API key — used to create/patch agents and tools and to run agent tests.
ELEVENLABS_API_KEY=

# The local tool server is exposed to the ElevenLabs cloud with a cloudflared quick tunnel
# (no account/token needed):  cloudflared tunnel --url http://localhost:8000
